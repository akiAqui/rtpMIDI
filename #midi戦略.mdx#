もしかすると以下の戦略が有効かも。どう思う？

three.js側では、いくつかの動作をあらかじめ定義しておく。その動作はBPMにより、動作する時間間隔が決まるが、音符として既に定義されている。例えば、二小節の間で、特定のtcbスプライン曲線上を移動という動作を作ってそのbehaviorの番号をきめておく。プログラムチェンジとnote-onを同時に送信し、オブジェクトの指定には、program changeとnote のノート番号として127^2個数のオブジェクトを指定できる、そして、behaviorの番号はvelocityと考えて、にして127種類にする。この戦略はどうか？


